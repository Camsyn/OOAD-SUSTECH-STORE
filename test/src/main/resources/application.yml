# DataSource Config
spring:
  datasource:
    # 数据库驱动：
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 数据源名称
    name: defaultDataSource
    #    schema: classpath:db/schema-h2.sql
    #    data: classpath:db/data-h2.sql
    # 数据库连接地址
    url: jdbc:mysql://camsyn.top:3306/oauth?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&useSSL=false&zeroDateTimeBehavior=convertToNull&serverTimezone=Asia/Shanghai
    username: root
    password: 123456

  redis:
    port: 6379
    host: localhost


  cache:
    cache-names: seme,inst
    redis:
      time-to-live: 10m

